name: replace
version: 1.2.3
description: An easy to use cross-platform regex replace command line util and pm CLI to modify pubspec.yaml files.
repository: https://github.com/robrbecker/replace

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.7.0
  cli_script: ^1.0.0
  file: ^7.0.1
  glob: ^2.1.3
  pub_semver: ^2.2.0
  pubspec_manager: ^4.0.1
  yaml: ^3.1.3

dev_dependencies:
  build_runner: ^2.13.1
  path: ^1.9.1
  pubspec_generator: ^5.0.1
  test: ^1.31.0

executables:
  replace:
  pm:
